13.mv全屏播放(video 控件全屏按钮)的时候下一首播放失效了，停在这首歌结束没下一首了
   - 已做:切歌前主动 exitFullscreen 防护 + _onEnded 加 console.debug 诊断日志
   - 待复现:失败时看 Console 有没有 "[player] audio ended → next";有=next 被挡(看报错),没有=audio ended 没触发
   - 优先级:低(使用场景少,想全程看画面可直接上 b 站)

--- 以下已完成(2026-08-29) ---
1.MV 模式最小化音频消失 → 修复:隐藏期间 pause 不联动停音频 + 恢复时补画面
2.音频切 MV 卡顿 → 修复:视频缓冲到 canplay 再开播追平音频,5s 兜底
3.播放全部按钮(随机模式随机起点)
4.空格暂停(输入框聚焦不拦截)
5.搜索不全 → 根因:匿名搜索缺登录态个性化排序;已带凭证搜索 + 搜索栏「个性」开关 + 设置同步开关
6.播放列表悬停显示详情浮层(全名/UP/时长/来源/原曲 + 替换歌曲入口)
7.随机模式下新加入歌曲默认下一首播放(设置开关 new_track_next_on_shuffle)
8.MV 更大(去 46vh 上限,16:9 撑满左栏)+ 删除空态提示文案
9.删除歌曲不触发播放列表滚动
10.长视频(>10min)不自动下载,只允许手动下载
11.替换歌曲浮层移到行正上方 + 400ms 延迟
12.轮询降频 2s→5s(缓存状态/匹配面板/匹配弹窗)
